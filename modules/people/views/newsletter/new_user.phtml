<?php 
$base_url = Config::get('system.base_url');
$url = $base_url.'!people/profile/index.';
$count = count($peoples);
$content = I18N::T( 'people', $template, ['%count'=>$count] );
foreach ($peoples as $people) {
	$arr[] = URI::anchor($url.$people->id, $people->name);
}
if (count($arr)) {$str= $content.implode(', ', $arr);}
?>
<li>➡ &#160;&#160;&#160;<?php echo $str; ?></li>