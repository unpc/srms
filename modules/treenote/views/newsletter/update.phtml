<?php
	$base_url = Config::get('system.base_url');
	$id = $note->project_id;
	$project = O('tn_project', $id);
	$url = $base_url.'!treenote/project/index.'.$id;
	$str = I18N::T('treenote', $template, ['%name' => URI::anchor($url, $project->title), '%count'=>$count]);
?>
<li> ➡ &#160;&#160;&#160;<?php echo $str ?></li>