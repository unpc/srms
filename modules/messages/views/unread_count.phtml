<?php
$me = L('ME');
$count = Q("message[receiver={$me}][is_read=0]")->total_count();
if ($count > 0): 
	if ($count > 99) {
		$count = '...';
		$len = 2;
	}
	else {
		$len = strlen($count);
	}
?>
	<span class="num_notif num_notif_<?php echo $len;?> middle"><?php echo $count; ?></span>
<?php endif; 
