<?php 

$me = L('ME'); 
if (!$me->id || !$me->is_active()) return;

if (Q("$me<incharge equipment")->total_count() > 0 && (!Module::is_installed('summary') || $me->access("[大数据体系]查看负责仪器的预约情况"))): ?>
	<a href="<?php echo URI::url('!equipments/extra/reserv'); ?>" class="blue"><?php echo I18N::T('eq_reserv', '您负责的所有仪器的预约情况'); ?></a>
<?php 
endif;
