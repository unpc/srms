<?php 
$me = L('ME');
/* BUG #1057::update信息为空的情况下，左侧还是会出现update的下划线(kai.wu@2011.08.19) */
$count = Update::count_updates($me);
if ($me->id && $me->is_active() && $count > 0): ?>
	<a href="<?php echo URI::url('!update'); ?>" class="blue"><?php echo I18N::T('update', '您有未查看的系统更新信息'); ?></a>
<?php endif;
