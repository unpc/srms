<?php
$incharges = Q("{$vidcam} user.incharge");
$i = 0;
if ($incharges->total_count()) :
    foreach($incharges as $incharge) :
        ?>
        <a href="<?php echo H($incharge->url() ) ?>"><?php echo H($incharge->name); ?></a><?= (++$i) < $incharges->total_count() ? ',' : ''?>
        <?php
        endforeach;
else :
?>
--
<?php endif;?>
