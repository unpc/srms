<div <?php echo $stock->parent->id;?>>
	<a href="<?php echo H($stock->url()) ?>"><strong><?php echo H($stock->product_name)?></strong></a>
<?php
$root = Tag_Model::root('inventory'); /* TODO fix inventory tag */
$tags = Q("$stock tag[root=$root]");
foreach ($tags as $tag) {
		echo '<a class="inventory_tag" href="'.URI::url('!inventory/index?tag='.$tag->id).'">'.H($tag->name).'</a> ';
}
?>
</div>
