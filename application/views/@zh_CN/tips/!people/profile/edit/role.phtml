<?php
$params = Config::get('system.controller_params');
$user = O('user', $params[0]);
?>
<div class="description">
	<p>您正在查看 <?=$user->name ?> 的组别。</p>
	<p>
		<span class="tip_star">*</span>
		<span class="tip_content">您可以把该用户加入到其他分组，如不设置，则默认该用户为普通用户。</span>
	</p>
	<p>
		<span class="tip_star">*</span>
		<span class="tip_content">管理员权限。</span>
	</p>
	<br />
</div>

